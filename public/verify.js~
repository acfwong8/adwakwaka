if(creds.getPermissions() !== 'superuser'){
    $(".main").empty();
    var $a = $("<a>").attr("href","/login").text("Unauthorized access. Log in to gain more permissions.")
    $(".main").append($a);
}
